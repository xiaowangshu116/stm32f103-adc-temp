我想设计一个单片机内部ADC采集温度的程序，
1、单片机型号：STM32F103CBT6，外部晶振10Mhz。
2、有三个指示灯（共阳极），IO口是PA8,PB14和PB12，到时候做成间隔一秒的亮灭。
3、温度信号输入到单片机内部ADC12_IN6(PA6)脚，实际温度值是该引脚采集到的电压值除以17.47除以0.625乘以1000减去100，最后再除以0.385.
4、串口每隔1秒发送ADC采到的数据以及计算后的温度值到上位机，并换行，格式是：ADC码值：（具体采集的值）。温度：（计算后的温度值）。串口的TX RX连单片机的PB6和PB7。

帮我用C语言写一个HAL工程代码，并编译成可执行文件，并通过ST-LINK下载到我的板子上。你先按我说的出个方案。

claude --dangerously-skip-permissions